// import 가져오기 pages - emotion 스타일을 적용할 폴더 
import styled from '@emotion/styled'

// exprot 스타일을 가진 변수 내보내기 `백틱 안에 효과 적용 `
export const Email = styled.div` 
    color: red;
`
export const EmailInput = styled.input`
    width: 300px;
`